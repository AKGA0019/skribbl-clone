# Local development usually needs no values.
# For split frontend/backend deployment, set VITE_SERVER_URL in client.
# For single-service deployment, set CLIENT_ORIGIN to your public frontend URL.
CLIENT_ORIGIN=http://localhost:5173
